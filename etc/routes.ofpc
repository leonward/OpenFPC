# Example OpenFPC route file.
##############################################################################
# http://www.openfpc.org
#
# Note: format change: Jan 31/2015. 
# Each node no longer requires its own password. The pass 'hash' of the original user is passed to the node 

Default_Node=localhost:4242
